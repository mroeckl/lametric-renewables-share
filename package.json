{
	"name": "lametric-renewables-share",
	"description": "This project defines an application for LaMetric Smart Clocks to visualize the current share of renewable energy in the electricity grid and an outlook for the next 10 hours.",
	"version": "1.0.0",
	"license": "MIT",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.7.5",
		"vitest": "~3.0.7",
		"wrangler": "^4.6.0"
	}
}
